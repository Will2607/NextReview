// Isolated example that conceptually represents app/dashboard/@analytics/page.tsx.
// @analytics is a named slot composed by the dashboard layout, not a URL segment.

export default function AnalyticsPage() {
  return (
    <section>
      <p>Analytics content appears in this slot.</p>
    </section>
  );
}
