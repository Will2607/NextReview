// Isolated example that conceptually represents app/dashboard/@team/page.tsx.
// @team is a named slot, not a URL segment.

export default function TeamPage() {
  return (
    <section>
      <p>Team activity appears in this slot.</p>
    </section>
  );
}
